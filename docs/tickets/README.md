# Tickets

Store local ticket requirements as one markdown file per ticket:

```text
docs/tickets/<ticketId>.md
```

Use `docs/templates/ticket.md` when creating a ticket requirement file. A
ticket is the input source for the ticket-driven Harness workflow; work packets,
proposals, implementation plans, validation evidence, and UAT cases are derived
from it.
