# Decisions

Use `docs/templates/decision.md` when adding a new decision.

Add a decision when:

- A high-risk feature chooses one design over another.
- A workflow, validation, or source-of-truth rule changes.
- A durable schema or command contract changes.
- A repeated ambiguity should become stable repo policy.
